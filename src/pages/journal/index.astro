---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { formatDate } from "../../utils/date";

const posts = (await getCollection("journal", ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<BaseLayout title="The Thinking Cap — Journal | Dean Weston" preloadImage="/img/sherlock-pop.webp">
    <section
        class="panel panel--intro panel-has-bg panel-has-bg--blue panel--centered"
      >
        <div class="panel__image panel__image--sherlock"></div>
        <div class="grid-wrapper content">
          <h1 class="content-col">
            <span class="pre-heading">The Thinking Cap</span>
            <span class="visually-hidden">&thinsp;&mdash;&thinsp;</span>
            <span class="heading--lg"
              >Dean Weston's journal on the web, technology and whatever else escapes my noggin</span
            >
          </h1>
        </div>
      </section>
    <section class="panel">
        <div class="grid-wrapper content">

          <div class="content-col content-col--narrow">
            {posts.length === 0 ? (
        <p>No posts yet.</p>
      ) : (
        posts.map((post) => (
            <article class="article underline-wave">
              <h2 class="article__heading">
                <a href={`/journal/${post.id}/`}>{post.data.title}</a>
              </h2>

              <p class="article__date">
                <time datetime={post.data.pubDate.toISOString()}>
                  {formatDate(post.data.pubDate)}
                </time>
              </p>

              {post.data.description && (
                <p class="article__desc">{post.data.description}</p>
              )}
            </article>
          ))
      )}
          </div>
        </div>
      </section>
    </BaseLayout>